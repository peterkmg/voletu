/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { SyncStatusQueryResponse } from '../../types/SyncTypes/SyncStatus.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '~/shared/api/kubb-client'
import fetch from '~/shared/api/kubb-client'

function getSyncStatusUrl() {
  const res = { method: 'GET', url: `/sync/status` as const }
  return res
}

/**
 * @description Returns node synchronization status and readiness information used by replication workers.
 * @summary Get sync status
 * {@link /sync/status}
 */
export async function syncStatus(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    SyncStatusQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: getSyncStatusUrl().url.toString(),
    ...requestConfig,
  })
  return res.data
}
