/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { CatalogWarehouseListQueryResponse } from '../../types/CatalogTypes/CatalogWarehouseList.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '~/shared/api/kubb-client'
import fetch from '~/shared/api/kubb-client'

function getCatalogWarehouseListUrl() {
  const res = { method: 'GET', url: `/catalog/warehouses` as const }
  return res
}

/**
 * @description Returns warehouses linked to bases.
 * @summary List warehouses
 * {@link /catalog/warehouses}
 */
export async function catalogWarehouseList(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    CatalogWarehouseListQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: getCatalogWarehouseListUrl().url.toString(),
    ...requestConfig,
  })
  return res.data
}
