/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { SystemHealthQueryResponse } from '../../types/SystemHealthTypes/SystemHealth.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '~/shared/api/kubb-client'
import fetch from '~/shared/api/kubb-client'

function getSystemHealthUrl() {
  const res = { method: 'GET', url: `/health` as const }
  return res
}

/**
 * @description Returns basic API liveness status used by orchestration and readiness probes.
 * @summary Health check
 * {@link /health}
 */
export async function systemHealth(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    SystemHealthQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: 'GET',
    url: getSystemHealthUrl().url.toString(),
    ...requestConfig,
  })
  return res.data
}
