/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import type { CatalogWarehouseListQueryResponse } from '../../types/CatalogTypes/CatalogWarehouseList.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '~/shared/api/kubb-client'
import { queryOptions, useQuery } from '@tanstack/react-query'
import { catalogWarehouseList } from '../../client/CatalogClient/catalogWarehouseList.ts'

export function catalogWarehouseListQueryKey() {
  return [{ url: '/catalog/warehouses' }] as const
}

export type CatalogWarehouseListQueryKey = ReturnType<
  typeof catalogWarehouseListQueryKey
>

export function catalogWarehouseListQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = catalogWarehouseListQueryKey()
  return queryOptions<
    CatalogWarehouseListQueryResponse,
    ResponseErrorConfig<Error>,
    CatalogWarehouseListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return catalogWarehouseList({
        ...config,
        signal: config.signal ?? signal,
      })
    },
  })
}

/**
 * @description Returns warehouses linked to bases.
 * @summary List warehouses
 * {@link /catalog/warehouses}
 */
export function useCatalogWarehouseList<
  TData = CatalogWarehouseListQueryResponse,
  TQueryData = CatalogWarehouseListQueryResponse,
  TQueryKey extends QueryKey = CatalogWarehouseListQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        CatalogWarehouseListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? catalogWarehouseListQueryKey()

  const query = useQuery(
    {
      ...catalogWarehouseListQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
