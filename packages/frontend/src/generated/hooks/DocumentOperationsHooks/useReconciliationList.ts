/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import type {
  ReconciliationListQueryParams,
  ReconciliationListQueryResponse,
} from '../../types/DocumentOperationsTypes/ReconciliationList.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '~/shared/api/kubb-client'
import { queryOptions, useQuery } from '@tanstack/react-query'
import { reconciliationList } from '../../client/DocumentOperationsClient/reconciliationList.ts'

export function reconciliationListQueryKey(params?: ReconciliationListQueryParams) {
  return [{ url: '/reconciliations' }, ...(params ? [params] : [])] as const
}

export type ReconciliationListQueryKey = ReturnType<
  typeof reconciliationListQueryKey
>

export function reconciliationListQueryOptions(
  params?: ReconciliationListQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = reconciliationListQueryKey(params)
  return queryOptions<
    ReconciliationListQueryResponse,
    ResponseErrorConfig<Error>,
    ReconciliationListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return reconciliationList(params, {
        ...config,
        signal: config.signal ?? signal,
      })
    },
  })
}

/**
 * @description Returns reconciliation documents. Supports in-memory query filtering and pagination.
 * @summary List reconciliations
 * {@link /reconciliations}
 */
export function useReconciliationList<
  TData = ReconciliationListQueryResponse,
  TQueryData = ReconciliationListQueryResponse,
  TQueryKey extends QueryKey = ReconciliationListQueryKey,
>(
  params?: ReconciliationListQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        ReconciliationListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey
    = resolvedOptions?.queryKey ?? reconciliationListQueryKey(params)

  const query = useQuery(
    {
      ...reconciliationListQueryOptions(params, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
