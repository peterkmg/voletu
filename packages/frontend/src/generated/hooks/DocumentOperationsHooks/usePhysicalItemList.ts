/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import type { PhysicalItemListQueryResponse } from '../../types/DocumentOperationsTypes/PhysicalItemList.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '~/shared/api/kubb-client'
import { queryOptions, useQuery } from '@tanstack/react-query'
import { physicalItemList } from '../../client/DocumentOperationsClient/physicalItemList.ts'

export function physicalItemListQueryKey() {
  return [{ url: '/physical-transfers/items' }] as const
}

export type PhysicalItemListQueryKey = ReturnType<
  typeof physicalItemListQueryKey
>

export function physicalItemListQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = physicalItemListQueryKey()
  return queryOptions<
    PhysicalItemListQueryResponse,
    ResponseErrorConfig<Error>,
    PhysicalItemListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return physicalItemList({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * {@link /physical-transfers/items}
 */
export function usePhysicalItemList<
  TData = PhysicalItemListQueryResponse,
  TQueryData = PhysicalItemListQueryResponse,
  TQueryKey extends QueryKey = PhysicalItemListQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        PhysicalItemListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? physicalItemListQueryKey()

  const query = useQuery(
    {
      ...physicalItemListQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
