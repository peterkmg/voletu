/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import type { SyncWatermarkListQueryResponse } from '../../types/SyncTypes/SyncWatermarkList.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '~/shared/api/kubb-client'
import { queryOptions, useQuery } from '@tanstack/react-query'
import { syncWatermarkList } from '../../client/SyncClient/syncWatermarkList.ts'

export function syncWatermarkListQueryKey() {
  return [{ url: '/sync/watermarks' }] as const
}

export type SyncWatermarkListQueryKey = ReturnType<
  typeof syncWatermarkListQueryKey
>

export function syncWatermarkListQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = syncWatermarkListQueryKey()
  return queryOptions<
    SyncWatermarkListQueryResponse,
    ResponseErrorConfig<Error>,
    SyncWatermarkListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return syncWatermarkList({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * @description Returns high-water marks tracked per target node and direction for incremental replication.
 * @summary List sync watermarks
 * {@link /sync/watermarks}
 */
export function useSyncWatermarkList<
  TData = SyncWatermarkListQueryResponse,
  TQueryData = SyncWatermarkListQueryResponse,
  TQueryKey extends QueryKey = SyncWatermarkListQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        SyncWatermarkListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? syncWatermarkListQueryKey()

  const query = useQuery(
    {
      ...syncWatermarkListQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
