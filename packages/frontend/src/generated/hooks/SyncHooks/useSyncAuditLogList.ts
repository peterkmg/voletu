/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from '@tanstack/react-query'
import type { SyncAuditLogListQueryResponse } from '../../types/SyncTypes/SyncAuditLogList.ts'
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from '~/shared/api/kubb-client'
import { queryOptions, useQuery } from '@tanstack/react-query'
import { syncAuditLogList } from '../../client/SyncClient/syncAuditLogList.ts'

export const syncAuditLogListQueryKey = () => [{ url: '/audit-logs' }] as const

export type SyncAuditLogListQueryKey = ReturnType<
  typeof syncAuditLogListQueryKey
>

export function syncAuditLogListQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = syncAuditLogListQueryKey()
  return queryOptions<
    SyncAuditLogListQueryResponse,
    ResponseErrorConfig<Error>,
    SyncAuditLogListQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return syncAuditLogList({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * @description Returns persisted local audit logs in ascending order for diagnostics and replication inspection.
 * @summary List audit logs
 * {@link /audit-logs}
 */
export function useSyncAuditLogList<
  TData = SyncAuditLogListQueryResponse,
  TQueryData = SyncAuditLogListQueryResponse,
  TQueryKey extends QueryKey = SyncAuditLogListQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        SyncAuditLogListQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? syncAuditLogListQueryKey()

  const query = useQuery(
    {
      ...syncAuditLogListQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}
